# requirements.txt

# Flask is a lightweight WSGI web application framework
Flask==2.1.1

# SQLAlchemy is the Python SQL toolkit and Object Relational Mapper
SQLAlchemy==1.4.32

# Flask-Migrate is an extension that handles SQLAlchemy database migrations for Flask applications using Alembic
Flask-Migrate==3.1.0

# Flask-Script is an extension that provides support for running external scripts in Flask
Flask-Script==2.0.6

# psycopg2 is a PostgreSQL database adapter for Python
psycopg2-binary==2.9.3

# Gunicorn is a Python WSGI HTTP Server for UNIX
gunicorn==20.1.0